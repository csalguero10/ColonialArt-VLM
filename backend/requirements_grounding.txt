# Isolated requirements for the grounding step (Stage 2 phrases -> bounding
# boxes). Installed into a SEPARATE virtual environment, never into the
# main site-packages — transformers>=4.44 (needed for Grounding DINO)
# conflicts with the transformers==4.38.1 pinned in the main
# requirements.txt for DeepSeek-VL. See README section on running this.

--extra-index-url https://download.pytorch.org/whl/cpu
torch
transformers==4.46.3
Pillow==10.2.0
pandas>=2.0.0
scipy>=1.11.0  # required by Owlv2ImageProcessor's post-processing