# --- PyTorch CPU-only ---
# IMPORTANT: this extra index must come before torch/torchvision below, or
# pip resolves to the default PyPI wheels, which pull the full NVIDIA CUDA
# stack (several GB) even though this project runs on CPU (Docker Desktop
# on Mac has no NVIDIA GPU to use it with). This is what was making the
# build download nvidia_cuda_runtime, nvidia_cufft, etc.
--extra-index-url https://download.pytorch.org/whl/cpu

# --- Web Framework & Server ---
fastapi==0.110.0
uvicorn[standard]==0.27.1
python-dotenv==1.0.1

# --- Procesamiento de Imágenes ---
Pillow==10.2.0           # Para el script metadata_pil.py
opencv-python-headless   # Para los recortes (crops) de YOLO

# --- Inteligencia Artificial (Vision & VLM) ---
ultralytics==8.1.0       # YOLOv8 para detección de sujetos
transformers==4.38.1     # Conexión con DeepSeek-VL
torch                    # Motor de IA — resuelto CPU-only via extra-index-url arriba
torchvision              # idem
accelerate               # Optimización de carga de modelos

# --- Manejo de Datos (Inventario) ---
pandas>=2.0.0            # Para crear y leer el CSV/Excel
openpyxl
requests>=2.31.0

# --- Scraping ---
playwright>=1.44
gspread>=6.0
google-auth>=2.30
beautifulsoup4>=4.12

# --- Vision & VLM (VORD2 pipeline: Claude + open-source VLMs + RAG) ---
openai>=1.40.0
tqdm>=4.66.0
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0
pypdf>=4.0.0
numpy>=1.24.0
trafilatura>=1.8.0
anthropic>=0.40.0
json-repair>=0.30.0